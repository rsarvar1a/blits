
[package]

name        = "lits"
version     = "0.1.0"
edition     = "2021"

[dependencies]

lazy_static = "1.4.0"
regex       = "1"
serde       = { version = "1.0", features = ["derive"] }
serde_json  = "1.0"

utils       = { path = "../utils" }
